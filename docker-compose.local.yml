version: "3"

services:
  client:
    build:
        context: ./client
        dockerfile: Dockerfile.develop
    restart: always
    ports:
      - 3000:3000

  socket:
    build:
        context: ./socket
        dockerfile: Dockerfile.develop
    restart: always
    ports:
      - 3001:3001

  

